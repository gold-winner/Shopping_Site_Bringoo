import { NgModule } from '@angular/core';

import { FormControlValidationTemplate } from './form-control-validation.template';

export const templates: Required<NgModule>['declarations'] = [FormControlValidationTemplate];
